<UserControl x:Class="WindowsDev.Views.Projects.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:UserCtrl="clr-namespace:WindowsDev.Controls.Users"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             dialogs:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d">

    <Grid Margin="15">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Project Info -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Back Button -->
                <Button Grid.Row="0"
                                Content="{DynamicResource Project_Button_Back}"
                                Width="100"
                                Margin="0,0,0,15"
                                HorizontalAlignment="Left"
                                mah:ControlsHelper.CornerRadius="8"
                                Command="{Binding SwitchToMainViewCommand}"/>

                <!-- Project Name -->
                <UserCtrl:HeaderTextBlock Grid.Row="1"
                                            Margin="0,0,0,10"
                                            CornerRadius="8"
                                            Header="{DynamicResource Project_Label_Name}"
                                            Text="{Binding Name}"/>

                <!-- Project Description -->
                <UserCtrl:HeaderTextBlock Grid.Row="2"
                                            CornerRadius="8"
                                            Header="{DynamicResource Project_Label_Description}"
                                            Text="{Binding Description}"/>
            </Grid>

            <!-- Right Panel: Tasks -->
            <StackPanel Grid.Column="1" >

                <!-- Action Buttons Row -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <!-- Add Task Button -->
                    <Button Content="{DynamicResource Project_Button_Add}"
                            mah:ControlsHelper.CornerRadius="8"
                            Margin="0,0,10,0"
                            Command="{Binding OpenDialogCommand}"/>

                    <!-- Delete Tasks Button -->
                    <Button Content="{DynamicResource Project_Button_Delete}"
                            mah:ControlsHelper.CornerRadius="8"
                            Command="{Binding DeleteSelectedTasksCommand}"/>
                </StackPanel>

                <!-- Search and Filter Row -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Input -->
                    <UserCtrl:HeaderTextBox Grid.Column="0"
                                                      CornerRadius="8"
                                                      Text="{Binding SearchFilter, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                      Watermark="{DynamicResource Project_Watermark_Search}"/>

                    <!-- Status Filters -->
                    <StackPanel Grid.Column="1" 
                                        Orientation="Horizontal" 
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center">
                        <CheckBox Content="{DynamicResource Project_Filter_All}"
                                          Margin="0,0,10,0"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding ShowAll}"/>
                        <CheckBox Content="{DynamicResource Project_Filter_Closed}"
                                          Margin="0,0,10,0"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding ShowClosed}"/>
                        <CheckBox Content="{DynamicResource Project_Filter_InProgress}"
                                          Margin="0,0,10,0"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding ShowInProgress}"/>
                        <CheckBox Content="{DynamicResource Project_Filter_Frozen}"
                                  VerticalAlignment="Center"
                                  IsChecked="{Binding ShowFrozen}"/>
                    </StackPanel>
                </Grid>

                <!-- Tasks Data Grid -->
                <DataGrid AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  ItemsSource="{Binding Tasks}">

                    <DataGrid.Columns>

                        <!-- Selection Checkbox Column -->
                        <DataGridTemplateColumn>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      Style="{DynamicResource MetroDataGridCheckBox}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- ID Column -->
                        <DataGridTextColumn Header="{DynamicResource Project_Label_Id}"
                                                Binding="{Binding Id}"
                                                Width="Auto"/>

                        <!-- Name Column -->
                        <DataGridTemplateColumn Header="{DynamicResource Project_Label_Name}" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextTrimming="CharacterEllipsis">
                                            <Hyperlink Command="{Binding DataContext.OpenTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                       CommandParameter="{Binding}">
                                                <Run Text="{Binding Name}"/>
                                            </Hyperlink>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Description Column -->
                        <DataGridTemplateColumn Header="{DynamicResource Project_Label_Description}" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}"
                                                   TextTrimming="CharacterEllipsis"
                                                   TextWrapping="NoWrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Deadline Column -->
                        <DataGridTextColumn Header="{DynamicResource Project_Label_Deadline}"
                                                Binding="{Binding DeadLineAtLocal, StringFormat=d}"
                                                Width="*"/>

                        <!-- Created Date Column -->
                        <DataGridTextColumn Header="{DynamicResource Project_Label_CreatedAt}"
                                                Binding="{Binding CreatedAt, StringFormat=d}"
                                                Width="*"/>

                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
