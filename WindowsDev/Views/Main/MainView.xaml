<UserControl x:Class="WindowsDev.Views.Main.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tabVM="clr-namespace:WindowsDev.ViewModels.Main.Tabs"
             xmlns:local="clr-namespace:WindowsDev.Views.Main.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Data templates for viewmodels -->
        <DataTemplate DataType="{x:Type tabVM:ProjectsViewModel}">
            <local:ProjectsTabView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type tabVM:SettingsViewModel}">
            <local:SettingsTabView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type tabVM:ProfileViewModel}">
            <local:ProfileTabView/>
        </DataTemplate>
    </UserControl.Resources>

    <!-- Main Tab Control -->
    <Grid>
        <TabControl Style="{DynamicResource MahApps.Styles.TabControl.AnimatedSingleRow}" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="{DynamicResource MainWindow_Label_Projects}" Content="{Binding Projects}"/>
            <TabItem Header="{DynamicResource MainWindow_Label_Settings}" Content="{Binding Settings}"/>
            <TabItem Header="{DynamicResource MainWindow_Label_Profile}" Content="{Binding Profile}"/>
        </TabControl>

    </Grid>
</UserControl>
