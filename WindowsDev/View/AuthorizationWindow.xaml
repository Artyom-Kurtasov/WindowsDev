<mah:MetroWindow x:Class="WindowsDev.View.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:UserCtrl="clr-namespace:WindowsDev.View.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsDev.View"
        mc:Ignorable="d"
        Title="Authorization" Height="450" Width="800" MinWidth="252">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,0">
            <iconPacks:PackIconMaterialDesign Kind="TaskRound" 
                                              HorizontalAlignment="Center" 
                                              Width="70" Height="70"/>
            <TextBlock x:Name="SigninTextBlock"
                       Text="Sign in to TaskManager" FontSize="14"/>
        </StackPanel>

        <UserCtrl:AdvancedTextBlock Grid.Row="1" Margin="0,20,0,0" 
                                    Header="Username"
                                    CornerRadius="8"
                                    HorizontalAlignment="Center"/>

        <Grid Grid.Row="2" HorizontalAlignment="Center" Width="250" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Password"
                   FontSize="14"
                   VerticalAlignment="Center"/>

                <TextBlock Text="Forgot password?"
                   Grid.Column="1"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Right"/>
            </Grid>

            <PasswordBox Grid.Row="1"
                 mah:ControlsHelper.CornerRadius="8"
                 Width="250"/>
        </Grid>

        <Button mah:ControlsHelper.CornerRadius="8" Margin="0,20,0,0" 
                Grid.Row="3" Width="250" >
            <WrapPanel>
                <TextBlock Text="Sign in" FontSize="12" FontWeight="Bold"/>
                <iconPacks:PackIconLucide Kind="LogIn" Margin="5,0,0,0" Height="13" FontSize="10" 
                                          VerticalAlignment="Bottom"/>
            </WrapPanel>
        </Button>
    </Grid>
</mah:MetroWindow>
