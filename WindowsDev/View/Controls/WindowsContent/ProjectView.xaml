<UserControl x:Class="WindowsDev.View.Controls.WindowsContent.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:UserCtrl="clr-namespace:WindowsDev.View.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:WindowsDev.View.Controls.WindowsContent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Project">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Button mah:ControlsHelper.CornerRadius="8" Grid.Row="0"
                                Content="Back"
                                Command="{Binding SwitchToMainViewCommand}"/>
                        
                        <UserCtrl:TextBlock BorderBrush="Aqua" BorderThickness="1" CornerRadius="8" Grid.Row="1"
                                            Header="Project's Name"
                                            Text="{Binding Name}"/>
                        <UserCtrl:TextBlock BorderBrush="Aqua" BorderThickness="1" CornerRadius="8" Grid.Row="2"
                                            Header="Project's Description"
                                            Text="{Binding Description}"/>
                    </Grid>

                    <DataGrid Grid.Column="1" Margin="5,0,0,0" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id"/>
                            <DataGridTemplateColumn Header="Name">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Hyperlink>
                                                <Run Text="{Binding Name}" Foreground="White"/>
                                            </Hyperlink>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Width="3*" Header="Description" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Description}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            
                                <DataGridTextColumn Header="Deadline"/>
                            <DataGridTextColumn Header="Created"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Project's settings"/>
        </TabControl>
        
        
    </Grid>
</UserControl>
