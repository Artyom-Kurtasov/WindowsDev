<UserControl x:Class="WindowsDev.View.Controls.WindowsContent.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsDev.View.Controls.WindowsContent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TabControl Style="{DynamicResource MahApps.Styles.TabControl.AnimatedSingleRow}">
                <TabItem Header="Projects">
                    <Grid>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Add" Background="Transparent" BorderBrush="Transparent"
                                        Command="{Binding OpenDialogCommand}"/>
                                <Button Content="Delete" Background="Transparent" BorderBrush="Transparent"/>
                            </StackPanel>

                            <DataGrid ItemsSource="{Binding ProjectList}"
                                      AutoGenerateColumns="false"
                                      SelectionMode="Extended"
                                      SelectionUnit="Cell"
                                      CanUserAddRows="False">
                                <DataGrid.Columns>
                                    <DataGridCheckBoxColumn ElementStyle="{DynamicResource MetroDataGridCheckBox}"
                                                            EditingElementStyle="{DynamicResource MetroDataGridCheckBox}"/>
                                    
                                    <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                                    
                                    <DataGridTemplateColumn Header="Name" Width="1*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock>
                                                    <Hyperlink Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                               CommandParameter="{Binding}">
                                                        <Run Text="{Binding Name}" Foreground="White"/>
                                                    </Hyperlink>
                                                </TextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    
                                    <DataGridTemplateColumn Width="3*" Header="Description" >
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Description}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                                        
                                    <DataGridTextColumn Header="Created" Binding="{Binding Created}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Grid>
                </TabItem>
                
                <TabItem Header="Settings"/>
            </TabControl>
        </StackPanel>

    </Grid>
</UserControl>
