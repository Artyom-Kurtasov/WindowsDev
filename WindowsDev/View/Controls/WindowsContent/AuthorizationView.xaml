<UserControl x:Class="WindowsDev.View.Controls.WindowsContent.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:UserCtrl="clr-namespace:WindowsDev.View.Controls"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsDev.View.Controls.WindowsContent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center">

            <!-- Logo -->
            <UserCtrl:Logo Kind="TaskRound"
                           ViewBoxHeight="70" ViewBoxWidth="70"
                           Text="Sign up to TaskManager"
                           FontSize="14"
                           HorizontalContentAlignment="Center"
                           HorizontalAlignment="Stretch" Margin="0,0,5,0"/>

            <!--Username-->
            <UserCtrl:AdvancedTextBlock
                Header="Username"
                CornerRadius="8"
                FieldMaxWidth="400"
                Margin="20,20,20,0" />

            <!--Password-->
            <Grid
                MaxWidth="400"
                Margin="20,20,20,0" >

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Password"
                   FontSize="14"/>

                    <TextBlock
                   Grid.Column="1"
                   HorizontalAlignment="Right">
                        <Hyperlink>
                            Forgot password?
                        </Hyperlink>
                    </TextBlock>
                </Grid>

                <PasswordBox Grid.Row="1"
                 mah:ControlsHelper.CornerRadius="8"
                 MinWidth="200" MaxWidth="400"
                 HorizontalAlignment="Stretch"/>
            </Grid>

            <!--SIGN IN BUTTON-->
            <Button mah:ControlsHelper.CornerRadius="8" Margin="20,20,20,0" 
                    MaxWidth="400" MinWidth="200">
                <TextBlock Text="Sign in" FontSize="12" FontWeight="Bold"/>
            </Button>

            <!--LINK TO REGISTRATION-->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        MaxWidth="400">

                <TextBlock>
                    <Run Text="New to TaskManager? "/>
                    <Hyperlink Command="{Binding ToRegView}">
                            Sign up
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
